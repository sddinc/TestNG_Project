


     overriding the
     toString() method,
     returns the desired output,
     depending upon
     our implementation.

     By overriding the toString()
     method of the Object class,
     we can return the
     values of the object,
     so we don't need
     to write much code.















     23