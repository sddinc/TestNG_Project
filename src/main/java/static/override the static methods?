
  ● No, we can't override static methods.Static belongs to class, not the object, so its implementation should not be changed
  from class to class.

  If a static method is created with a similar method
    signature as a method being passed down by inheritance this will be method hiding, not overriding