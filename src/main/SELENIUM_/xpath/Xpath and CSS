
    ● Generally CSS is easy
    to use and readable
    over XPATH.

    ● CSS works only in
    forward direction
    while with xpath,
    we can search elements
    backward or forward

    ● Xpath can work
    with text while CSS cannot

    CSS is much faster than XPATH